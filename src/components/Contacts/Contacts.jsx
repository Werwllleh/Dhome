import React from "react";
import s from "../Contacts/Contacts.module.css";

const Contacts = () => {
  return (
    <div id="contacts">
      <div>CONTACTS</div>
    </div>
  );
};

export default Contacts;
